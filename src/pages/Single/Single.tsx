import { FC } from "react";
import { withLayout } from "../../layout/Layout";

import "./Single.module.css";

const Single: FC = () => {
  return <main className="main">Single</main>;
};
export default withLayout(Single);
